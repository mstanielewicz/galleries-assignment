export type ButtonProps = {
  disabled?: boolean;
  onClick: () => void;
  label: string;
  selected?: boolean;
  width?: string;
};

export type SButtonProps = {
  disabled?: boolean;
  selected?: boolean;
  width?: string;
};
